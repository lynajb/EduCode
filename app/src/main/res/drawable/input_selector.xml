<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State: When the user clicks inside (Focused) -> Blue Border -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF"/>
            <corners android:radius="12dp"/>
            <stroke android:width="2dp" android:color="#2196F3"/> <!-- Blue Border -->
            <padding android:left="16dp" android:right="16dp"/>
        </shape>
    </item>

    <!-- State: Default (Not clicked) -> Gray Border -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF"/>
            <corners android:radius="12dp"/>
            <stroke android:width="1dp" android:color="#DDDDDD"/> <!-- Gray Border -->
            <padding android:left="16dp" android:right="16dp"/>
        </shape>
    </item>
</selector>